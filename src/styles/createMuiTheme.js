import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fff'
    },
    secondary: {
      main: '#FFA500'
    }
  },
  typography: {
    fontFamily: 'Roboto',
    fontSize: 14
  }
})
